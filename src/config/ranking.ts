/** Point values and thresholds for player rankings. */

export const RANKING_POINTS = { win: 3, loss: 1 } as const;

/** Minimum games required to appear in the Star players highlight. */
export const MIN_GAMES_FOR_STAR_PLAYER = 2;

/** Max rows shown per page on the Stats player rankings table. */
export const RANKINGS_PAGE_SIZE = 20;
